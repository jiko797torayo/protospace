//fasが色付き、farが白のハート
- if prototype.like_user(current_user.id)
  = button_to prototype_like_path(prototype_id: prototype.id, id: current_user.id), method: :delete, remote: true do
    = icon('fas', 'heart', class: 'red')
    %span.red
      = prototype.likes.count
- else
  = button_to prototype_likes_path(prototype), remote: true do
    = icon('far', 'heart', class: 'red')
    %span.red
      = prototype.likes.count